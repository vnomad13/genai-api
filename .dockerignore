# NOTE: weights/ is deliberately NOT ignored - the image ships the trained models.
# Exception: the GPT-2 checkpoints are 480 MB each and the API never loads them.
weights/gpt2_*/
.venv/
data/
.git/
.gitignore
__pycache__/
*.py[cod]
*.egg-info/
.uv/
dist/
build/
*.log
